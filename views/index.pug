head
    include includes/head.pug
body
    include includes/header.pug

    main 
        #foods
            ul
                each item in items
                    li
                        .card
                            img(src=`${item.image}`, alt=`'${item.name}' image`, onclick=`location.href = '/${item._id}';`)
                            h3=item.name
                            p=item.discription
                            .tools
                                a(href=`/delete/food/${item._id}`)
                                    i(class="tiny material-icons delete") delete
                                a(href=`/rewrite/food/${item._id}`)
                                    i(class="tiny material-icons create") create
                                a(onclick='save("' + item._id + '")')
                                    i(class="tiny material-icons save") save
        
        script.

            function save(id) {
                console.log(id);/*
                let data = JSON.parse(window.localStorage.getItem('save'));
                data.arr.push(id);
                window.localStorage.setItem('save', JSON.stringify(data));*/
            }


    include includes/footer.pug
